---
---

<footer class="ms-section ms-footer" data-theme="dark">
  <div class="ms-container ms-footer__inner">
    <div class="ms-footer__brand">
      <strong>MoodScale EU</strong>
      <p>
        An initiative for mental well-being, climate resilience and the UN
        Sustainable Development Goals.
      </p>
    </div>
  </div>
</footer>

<style>
  .ms-footer {
    position: relative;
    overflow: clip;
    padding-block: 0 var(--sp-6);
    background: linear-gradient(to bottom, #061610 0%, #050f08 100%);
  }
  .ms-footer__inner {
    display: block;
  }
  .ms-footer__brand {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: var(--sp-2);
  }
  .ms-footer__brand strong {
    font-family: var(--font-display);
    font-size: var(--fs-h3);
    color: var(--ms-on-dark);
  }
  .ms-footer__brand p {
    color: var(--ms-on-dark-soft);
    font-size: var(--fs-body);
    line-height: var(--lh-body);
    margin: 0;
  }
</style>
